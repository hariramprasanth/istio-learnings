apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: orders-virtual-service
spec:
  hosts:
  - "customers-service"
  http:
    - route:
      - destination:
          host: orders-service
          port: 
            number: 80
